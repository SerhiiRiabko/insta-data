# ============================================
# INSTA-DATA BACKEND — Dependencies
# ============================================

# 🔵 FastAPI & Web
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# 🗄️ Databases
motor==3.3.2           # Async MongoDB driver
pymongo==4.6.0
sqlalchemy==2.0.23     # ORM for PostgreSQL
psycopg2-binary==2.9.9 # PostgreSQL adapter
alembic==1.12.1        # Database migrations

# 🔴 Cache
redis==5.0.1

# 🔐 Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1           # pinned: passlib 1.7.4 breaks on bcrypt>=4.1's __about__ removal
python-multipart==0.0.6
pydantic-extra-types==2.2.0
email-validator==2.3.0  # required by pydantic's EmailStr (auth.py request models)

# 📧 Email (magic-link login) — same Resend account already used by
# MonteLand/KartIQ (RESEND_API_KEY shared across the user's local projects)
resend==2.4.0

# 📱 Instagram Scraping
# instagrapi pins pydantic 1.x, which conflicts with the pydantic v2 stack
# the rest of this app runs on - deliberately NOT installed (see the
# try/except ImportError guards in services/__init__.py and router.py that
# keep the real Instagram scraper optional). pytesseract/pillow (OCR) were
# only used by that same disabled pipeline, so they're out too.

# 🌐 Web Scraping
playwright==1.40.0
beautifulsoup4==4.12.2
requests==2.31.0
aiohttp==3.9.1

# 📊 Data Processing
pandas==2.1.3
numpy==1.26.2

# 📝 Logging & Monitoring
structlog==23.2.0
python-json-logger==2.0.7
sentry-sdk==1.38.0

# 🧪 Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2

# 🔧 Code Quality
black==23.12.0
ruff==0.1.8
mypy==1.7.1
pylint==3.0.2

# 🛠️ Utilities
python-dotenv==1.0.0
click==8.1.7
typer==0.9.0

# 🎯 Fuzzy String Matching (for product grouping)
fuzzywuzzy==0.18.0
python-Levenshtein==0.23.0
rapidfuzz==3.5.2

# ⏰ Scheduling (weekly price refresh)
apscheduler==3.11.3