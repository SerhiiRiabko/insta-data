/**
 * i18n Configuration for next-intl
 * Supports: Ukrainian (ukr), Russian (rus), Montenegrin (mne),
 * Serbian (srb), Bosnian (bos), English (eng)
 */

/**
 * i18n Configuration (Legacy - using next-intl.config.ts instead)
 * This file is kept for reference but the actual config is in next-intl.config.ts
 */

export const locales = ['ukr', 'rus', 'mne', 'srb', 'bos', 'eng'] as const;
export const defaultLocale = 'ukr' as const;